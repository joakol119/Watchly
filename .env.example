# Base de datos
POSTGRES_USER=watchly
POSTGRES_PASSWORD=cambia_esto_en_produccion
POSTGRES_DB=watchly

# Backend
JWT_SECRET=una_clave_secreta_muy_larga_y_segura
TMDB_API_KEY=tu_api_key_de_tmdb_aqui

# CORS (separados por coma, sin espacios)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# Frontend (URL pública del backend en producción)
NEXT_PUBLIC_API_URL=http://localhost:4001
